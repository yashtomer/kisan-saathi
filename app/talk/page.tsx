import LandingPage from '@/components/LandingPage';

/**
 * Farmer-facing conversation on its own page — used for direct links and as
 * the standalone entry point when someone is not coming via the dashboard.
 */
export default function TalkPage() {
  return <LandingPage />;
}
